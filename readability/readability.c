#include <cs50.h>
#include <stdio.h>
#include <string.h>
void calculate_index(string t);

int main(void)
{
    string text = get_string("Text: ");
}

void calculate_index(string t)
{
    int length = strlen(t);
    int letters = 0;

    for (int i = 0; i < length; i++)
    {
        
        if (t[i] >= 'A' && t[i] <= 'Z')
    }
}
